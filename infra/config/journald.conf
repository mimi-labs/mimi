# infra/config/journald.conf - Strict RAM-Only Policy

[Journal]
# CRITICAL: Do not write logs to disk
Storage=volatile 

# Cap RAM usage to prevent DoS
SystemMaxUse=50M
RuntimeMaxUse=50M

# Disable forwarding
ForwardToSyslog=no
ForwardToWall=no
ForwardToKMsg=no
ForwardToConsole=no
